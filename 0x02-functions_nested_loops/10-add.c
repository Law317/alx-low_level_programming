#include "main.h"
/**
 * add -  a function that adds two integers and returns the result.
 * @c: argument
 * @d: argument
 * Return: Always 0.
 */

int add(int c, int d)
{
	int j;

	j = c + d;
	return (j);
}
